# Traffic Monitor Module
This module is responsible for collecting idle bandwidth and triggering dynamic bandwidth offers to traffic customers.
Use `snmp_monitor.py` to monitor interface bandwidth consumption via SNMP.
Install dependency: `pip install pysnmp`
Example usage: `from snmp_monitor import sample_as_dict`
